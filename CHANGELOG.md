# 更新日志

本文件记录面向使用者的重要变化。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.7] - 2026-08-13

### 修复

- 重写 WebKitGTK 4.0 试验包校验流程，不再用无输出的 `test` 和固定路径静默失败；
- 校验器会明确输出实际产物路径、主机与 DEB 架构、动态链接库、DEB 依赖和最高 glibc 符号，并为每种失败给出具体原因；
- 试验构建校验失败时上传带 `unverified-diagnostics` 标记的诊断产物和完整校验报告，便于定位 ARM64 等平台特有问题。

## [0.2.6] - 2026-08-12（试验构建，未公开发布）

### 新增

- 新增独立的 Tauri 1 / WebKitGTK 4.0 Linux 试验外壳，与正式 Tauri 2 Reader 共用界面、搜索和离线知识库格式；
- 新增手动 GitHub Actions 工作流，原生生成 x64 与 ARM64 的 DEB、AppImage 试验包；
- 试验工作流在 Debian 10 / glibc 2.28 环境中构建，并检查二进制没有误链接 WebKitGTK 4.1 或更高版本的 glibc 符号。

### 调整

- Reader Web 前端的构建目标调整为 Safari 13 级别，避免旧版 WebKitGTK 4.0 无法解析较新的 JavaScript 语法；
- Tauri 1 与 Tauri 2 Reader 使用相同应用标识，可以继续读取此前记住的知识库路径。

## [0.2.5] - 2026-08-11

### 修复

- Reader 不再把每个有序列表项都显示为 `1.`，能够还原连续编号、手动起始编号、`auto` 续号和嵌套列表的独立编号；
- Exporter 将飞书有序列表的 `style.sequence` 保存在页面 JSON 中，避免精确编号信息在导出阶段丢失；
- 打开不含 `sequence` 的旧离线知识库时，Reader 会根据相邻有序列表块推断 1、2、3，并在列表中断后重新从 1 开始。

### 调整

- Reader 页面缓存状态升级到 version 4，首次使用本版增量导出时自动重新生成包含有序列表序号的页面 JSON；
- Reader workflow 增加有序列表编号逻辑测试。

## [0.2.4] - 2026-08-11

### 修复

- 正确解析新旧版子页面目录块的 `wiki_token`，多层导航页可以按目标分类节点还原 b1、b2、b3 等实际子页面，不再错误地只显示 b 或生成空目录；
- 飞书未返回可用 `wiki_token` 时，前置标题匹配会展开分类节点的子页面，而不是把分类节点本身误作目录内容；
- Reader 页面结构签名覆盖完整子树，新增、删除、改名或调整下级页面后，依赖这些层级的上级导航页不再错误复用旧缓存。

### 调整

- Reader 页面缓存状态升级到 version 3，首次使用本版增量导出时自动重新生成旧缓存页面；
- 子页面解析诊断升级到 version 2，记录每个目录块的 `wiki_token`、目标节点、解析策略、最终链接和未解决原因。

## [0.2.3] - 2026-08-11

### 修复

- 替换被截断的 Noto Sans SC Regular 字体，修复 Exporter 和 Reader 部分中文字符缺失的问题。

### 调整

- 新增嵌入字体的结构、字形数量和 SHA-256 完整性检查，并在 Exporter 与 Reader 的 GitHub Actions 编译前执行。

## [0.2.2] - 2026-08-11

### 新增

- Reader 记住最后一次成功打开的离线知识库，下次启动时自动加载，仍可随时切换；
- 页面顶部提供可点击的层级面包屑，左侧目录会自动展开、定位并高亮当前页面；
- 离线包增加子页面列表解析诊断文件，记录无法可靠匹配的目录块。

### 调整

- 多个子页面列表分别按真实链接、嵌套标题和前置标题匹配，不再只还原第一个列表；
- 33 号 View 块作为透明容器显示其子内容；无可恢复内容的未知小组件不再在正文中显示警告，但仍保留诊断记录；
- Reader 页面缓存状态升级，首次使用本版导出时会重新生成旧缓存页面。

## [0.2.1] - 2026-08-10

### 修复

- 修复测试代码触发 xUnit2031 分析器规则，导致 GitHub Actions 编译失败的问题。

## [0.2.0] - 2026-08-10

### 新增

- 导出设置提供“Reader 离线包”“Office 文档”“Reader + Office”三种输出模式，默认生成紧凑 Reader 离线包；
- Reader 离线包直接保存飞书页面块结构，不再依赖 DOCX 二次转换；
- Reader 支持块级段落换行、标题、列表、引用、代码、图片、附件、页面目录、子页面目录和内部页面跳转；
- 离线知识库格式升级至 version 3，Reader 继续兼容 version 1 和 version 2；
- Reader 与 Office 使用独立增量状态，并按飞书来源保存在导出根目录的 `.feishu-exporter-state` 中。

### 调整

- Reader 完整保留所有飞书页面；导航页判断只影响 Office 文件是否生成；
- Reader 离线包不再复制整套 Office 文件，只保留页面 JSON 和阅读所需图片、附件；
- 导出成功后隐藏“开始导出”，改为“打开输出位置”，并提供弱化的“返回导出设置”和“查看导出摘要”；
- 标题命中继续严格优先于仅正文命中的搜索结果。

## [0.1.5] - 2026-08-10

### 修复

- 40 号 AddOns 小组件不再被无条件视为实际内容；成组对应的标题与子页面目录会把目录型小组件识别为导航结构；
- 无法确认用途的 AddOns 小组件进入“无法确定”组并默认保留；
- 导航分析诊断文件改为直接写入中文，不再把汉字显示为 `\\uXXXX`。

## [0.1.4] - 2026-08-10

### 修复

- 修复导航链接表格分析中的可空结构体 LINQ 表达式无法通过 C# 编译的问题。

## [0.1.3] - 2026-08-10

### 新增

- 导航页审核改为“高度疑似导航页、无法确定、明确有正文”三级分类；
- 审核窗口默认勾选高度疑似导航页，无法确定的项目默认保留；
- 在 `.feishu-export/navigation-analysis.json` 中记录块类型、正文指标、分类理由和分析错误。

### 修复

- 改为利用飞书块的 `parent_id` 和 `children` 分析块树，不再把表格单元格或导航组件内部的文字误算成页面正文；
- 链接内容与标题或子页面相符的表格改为待人工确认，不再直接作为业务表格证明页面有正文；
- 未知块类型和内容分析失败不再隐藏在审核清单之外。

## [0.1.2] - 2026-08-09

### 新增

- 离线知识库改用飞书页面语义树，页面可以同时拥有正文和子页面；
- Reader 为有子页面的正文页提供独立的展开箭头和可点击标题；
- Reader 继续兼容旧版 version 1 离线知识库。

### 修复

- 修复有正文的父页面在 Reader 中被拆成同名文档和文件夹且顺序不相邻的问题；
- 导航页判断只控制正文文件是否导出，不再参与 Reader 层级推断。

## [0.1.1] - 2026-08-09

### 新增

- 支持按连续正文块、累计字数和富内容识别疑似导航页；
- 增加导出前审核清单，可逐项决定是否跳过疑似导航页。

### 修复

- 修复把所有有子文档的页面一律视为导航页、导致正文页面未导出的问题；
- 文档块分析失败或遇到未知类型时改为保留导出，避免误跳过文档。

## [0.1.0] - 2026-08-09

### 新增

- 提供 Windows、常规 Linux 和 Alpine Linux 的飞书知识库导出工具；
- 支持知识库与云空间文件夹、增量状态、普通附件和文档内嵌附件；
- 支持生成保留目录层级和节点顺序的离线知识库；
- 提供 Windows 与 Linux 离线阅读器，支持标题优先的全文搜索；
- 建立统一版本号、程序内版本显示和带版本号的发布产物名称。
